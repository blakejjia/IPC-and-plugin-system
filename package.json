{
  "name": "my-plugin",
  "version": "1.0.0",
  "description": "一个模拟插件",
  "main": "./plugins/my_plugin.js",
  "activationEvents": [
    "onCommand:helloworld.helloWorld"
  ],
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/core_app.js",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
